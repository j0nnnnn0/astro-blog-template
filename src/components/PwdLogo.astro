<script>
    import { gsap } from "gsap";

    gsap.from(
 
    ".stagger-box", {
        x: 150,
        y: 150,
        opacity: 0,
        duration: 2.5,
        ease: "power1.inOut",
        rotation: 120,
        borderRadius: "100%",
        stagger: {
          amount: 0.5,
          grid: [6, 3],
          axis: "x",
          ease: "circ.inOut",
          from: "center",
        },
        repeat: 0,
      });

</script>

<h2 class="stagger-box mt-1 font-bold  flex justify-center items-center ">p</h2>
<h2 class="stagger-box mt-1 font-bold  flex justify-center items-center ">w</h2>
<h2 class="stagger-box mt-1 font-bold  flex justify-center items-center ">d</h2>
<h2 class="stagger-box mt-1 flex items-center text-green-600">.</h2>
<h4 class="stagger-box mt-1 flex items-center text-green-600">d</h4>
<h4 class="stagger-box mt-1 flex items-center text-green-600">e</h4>
<h4 class="stagger-box mt-1 flex items-center text-green-600">v</h4>